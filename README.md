# VIBE-CADING

# ⚠ This project is under active development. Functionality is not guaranteed.

> Point. Label. Vibe with AI.

OpenSCAD is powerful but describing geometry to an AI is hard.
"The top face", "that edge near the hole" — language fails where
space is precise.

VIBE-CADING bridges that gap. Load your SCAD file, click a face,
give it a name. The tool compiles and renders your model directly
in the browser — no local OpenSCAD installation required. The
structured JSON output gives your AI collaborator exact geometric
context — normals, coordinates, semantics — so you can stop
describing and start building.

---

The author has an architectural background, and the project code was largely completed with the assistance of an LLM. The author was primarily responsible for project planning and interaction logic design.

AGENT is already capable of creating SCAD files quite well. However, there is a lack of a communication bridge between humans and AI in the SCAD workflow. This is the motivation behind this project.

---

## Requirements

- [Python 3](https://www.python.org/downloads/)
- Chrome or Edge (recommended)
- Internet connection on **first run only** (downloads the OpenSCAD WASM engine, ~20 MB, cached locally)

> No local OpenSCAD installation needed.

---

## Quick Start

### Windows

1. **Start the server**

   Double-click `build.bat`, or run in Command Prompt:
   ```
   build.bat
   ```
   On first run, the server automatically downloads the OpenSCAD WASM engine (~20 MB). This only happens once.

2. **Open the viewer**

   Visit in your browser:
   ```
   http://localhost:8080/viewer/index.html
   ```

3. **Load and annotate your model**

   - Click **Monitor SCAD** — select your `.scad` file from the `model/` directory
   - The browser compiles and renders the model automatically
   - Save your `.scad` file anytime — the browser detects changes and recompiles within ~2 seconds
   - Click faces or edges in the 3D view → fill in the label → click **Save JSON**

4. **Start working with AI**

   Open a new AI session and send:
   ```
   Please read model/AI_CONTEXT.md and follow its instructions.
   Current model: example
   ```
   The AI will load the workflow rules, read the current SCAD source and JSON annotations, then wait for your instructions.

---

### WSL / Linux / macOS

1. **Start the server**
   ```bash
   bash build.sh
   ```

2. **Open the viewer**
   ```
   http://localhost:8080/viewer/index.html
   ```

3. Steps 3 and 4 are identical to Windows above.

> **WSL note:** The browser must access the tool via `http://localhost:8080`. Do not open `index.html` directly as a file.

---

## Standard Workflow

```
① Edit example.scad (manually or via AI)
        ↓
② Browser auto-detects the change (~2 s) and recompiles in-browser
   No build step needed.
        ↓
③ Browser: http://localhost:8080/viewer/index.html
   → Click "Monitor SCAD" → loads and renders the model
        ↓
④ Click faces / edges → add labels → Save JSON
        ↓
⑤ Tell your AI: "Add a hole on face_001..."
   (AI reads the latest JSON first, then edits with exact geometric context)
        ↓
⑥ Back to ①
```

---

## File Structure

```
Vibe-Cading/
├── README.md
├── server.py          # Local HTTP server + REST API
├── build.sh           # Start server (WSL/Linux/macOS)
├── build.bat          # Start server (Windows)
├── wasm/              # OpenSCAD WASM engine (auto-downloaded on first run)
├── viewer/
│   ├── index.html     # 3D annotation viewer
│   └── worker.js      # Web Worker: compiles SCAD via WASM
└── model/
    ├── AI_CONTEXT.md  # AI workflow rules — tell AI to read this at session start
    ├── example.scad   # Example model
    └── example.json   # Geometry annotations (generated by the viewer)
```

---

## How the AI Integration Works

`model/AI_CONTEXT.md` is a fixed workflow document. At the start of each AI session:

1. Tell the AI: *"Read `model/AI_CONTEXT.md` and follow its instructions."*
2. The AI reads the current SCAD source and JSON annotations from the local server
3. The AI uses annotation IDs (`face_001`, `edge_a`) as geometric references in all code changes
4. After modifying SCAD geometry, the AI automatically updates the JSON — fixing recoverable annotations and flagging others as invalid
5. Invalid annotations appear with a **⚠ warning** in the viewer — delete and re-annotate as needed

---

## Acknowledgements

**Reference Projects**

- [CADAM (Adam-CAD/CADAM)](https://github.com/Adam-CAD/CADAM) — React + Three.js rendering pipeline and Claude API architecture
- [FluidCAD (Fluid-CAD/FluidCAD)](https://github.com/Fluid-CAD/FluidCAD) — face/edge query and filter API design
- [Pointer-CAD (CVPR 2026)](https://arxiv.org/abs/2506.01012) — research confirming that face/edge selection is the core challenge in LLM-assisted CAD workflows

**Frameworks & Tools**

- [Three.js](https://threejs.org/) — 3D rendering and interaction in the browser
- [openscad-wasm](https://github.com/openscad/openscad-wasm) — OpenSCAD compiled to WebAssembly, enabling in-browser SCAD compilation
- [Python](https://www.python.org/) — local HTTP server
